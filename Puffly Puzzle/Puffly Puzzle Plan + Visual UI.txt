Puffly Puzzle PlanAct as an expert React and Tailwind CSS engineer. We are ready to build out the third tab of the Puffly multiplayer game engine - Puffly Puzzle. I want to build an interactive, intergenerational 16-piece jigsaw puzzle game using the image file, Puffly Puzzle V2.SVG image. Puffly Puzzle will have two modes the same way that we did for Puffly Checkers and Puffly Four-in-a-Row games. The Puffly Mode will include Puffly collaborating with the user to build the puzzle. The Friend Mode with include the Blue Team and the Green Team building the puzzle together. 1. GRID ARCHITECTURE: - Create a 4x4 grid workspace centered on the screen. - Use an inline SVG `<defs>` block with an `<clipPath id="puzzle-piece-mask" clipPathUnits="objectBoundingBox">` to dynamically mask the main image into 16 interlocking puzzle pieces. 2. SPLIT WORKSPACE TRAYS:- Implement a split holding tray layout on either side of the grid canvas.- Left Tray: Holds 8 randomly shuffled pieces for Player 1 Ð Puffly or the Blue Team.- Right Tray: Holds the remaining 8 randomly shuffled pieces for Player 2 Ð Green Team3. INTERACTIVE PHYSICS & AUDIO:- Use Framer Motion or a standard React drag-and-drop mechanism.- Implement a 10% "magnetic snap-to-grid" threshold. When a piece is dropped close to its true coordinate, snap it into place, lock it, and trigger an audio placeholder function `playSnapSound()`.- Add a subtle yellow highlight border to any piece that belongs to the outer edge of the puzzle matrix.4. UI STYLING:- Apply a clean, responsive layout. Ensure the canvas and trays scale nicely on desktop and iPad views.